// CONFIG

// Show all info, even if they are empty
const bool SHOWALL = false;

// Player list
// Size of player heads (in pixels) [20-100]
const double PLAYERHEADSISE = 50;

// determines the maximum number of players that the application can display in a list (0 to display the entire list)
const int MAXPLAYERSLIST = 0;

// determines if the player list is always shown (never hidden in an expander)
const bool AUTOEXPAND = false;